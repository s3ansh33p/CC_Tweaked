local chatBox = peripheral.find("chatBox")

-- send message that "s3nsuous was shot by Skeleton"
chatBox.sendMessage("s3nsuous was shot by Skeleton", "")
-- send to the same player
-- chatBox.sendMessageToPlayer("s3nsuous was shot by Skeleton", "s3nsuous")